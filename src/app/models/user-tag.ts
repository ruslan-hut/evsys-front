export interface UserTag {
  username: string,
  user_id: string,
  id_tag: string,
  source: string,
  is_enabled: boolean,
  local: boolean,
  note: string,
  date_registered: string,
  last_seen: string,
}
